<resources>
    <!-- Main Theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/accentColor</item>
        <item name="android:windowBackground">@color/backgroundColor</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textColorSecondary">@color/textSecondaryColor</item>
    </style>
    <style name="SpinnerStyle">
        <item name="android:textColor">#000000</item> <!-- Black Text Color -->
        <item name="android:dropDownSelector">@android:color/darker_gray</item> <!-- Darker dropdown selector -->
    </style>
    <style name="MenuTextAppearance" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">#000000</item> <!-- Black color -->
        <item name="android:background">#A9A9A9</item>
    </style>
    <style name="roundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item> <!-- Adjust corner size as needed -->
    </style>
    <!-- Custom Button Style -->
    <style name="CustomButtonStyle">
        <item name="android:background">@color/buttonColor</item>
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:paddingStart">2dp</item>
        <item name="android:paddingEnd">2dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:elevation">2dp</item>
    </style>

    <!-- Custom Text Style -->
    <style name="CustomTextStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Action Bar Style -->
    <style name="ActionBarStyle" parent="Widget.MaterialComponents.ActionBar.Solid">
        <item name="android:background">@color/primaryColor</item>
        <item name="android:textColor">@color/textColor</item>
    </style>
    <style name="ToolbarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">

        <item name="android:textColor">#ffffff</item> <!-- Change color as needed -->
        <item name="android:textSize">20sp</item> <!-- Adjust size as needed -->
    </style>

</resources>
